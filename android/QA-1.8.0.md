# 1.8.0 验证记录

验证日期：2026-09-07（中国时间）。版本号 1.8.0 / versionCode 9。

签名校验通过，并在测试模拟器完成正式包 1.7.0 → 1.8.0 覆盖安装；证书保持一致。

APK：`output/xia-gu-bp-polished-secure-v1.8.apk`

SHA-256：`f588bde3ff7f66dc13c65eebc6c41b4db43855f3328004a8d92ce367e7a06f49`

## 自动测试

- 23 项 JVM 单元测试：全部通过。覆盖推荐正负向关系、反向端点、BAN 与不想玩过滤、分路概率、全局分路分配、非常规阵容留空、手动换路、识别框边界等。
- `testDebugUnitTest assembleDebug assembleRelease`：通过；Release 包执行 R8 混淆、资源收缩和 lintVital。
- API 原生端到端测试：天元之弈 132 位英雄、2026-09-07 梯度、5 位选中英雄的关系请求成功，取得 667 行关系；原有五敌方示例的游走推荐首位为庄周。此结论仅对应本次数据快照，不对未来版本锁定排名。

## 头像回归

使用已有 BP 截图的边缘保真变体（中央内容横向适配，头像本身等比缩放），测试：

| 测试尺寸 | 已选英雄集合 | BAN 英雄集合 |
| --- | --- | --- |
| 2044×1000 | 通过 | 通过 |
| 1333×1000（约 4:3） | 通过 | 通过 |
| 1500×1000（3:2） | 通过 | 通过 |
| 1600×1000（16:10） | 通过 | 通过 |
| 1778×1000（约 16:9） | 通过 | 通过 |
| 2167×1000（约 19.5:9） | 通过 | 通过 |
| 2222×1000（约 20:9） | 通过 | 通过 |
| 2333×1000（约 21:9） | 通过 | 通过 |
| 2667×1000（约 24:9） | 通过 | 通过 |
| 2222×1000 + 左侧 70 px 黑边 | 通过 | 通过 |

模拟器 Android 11 / x86_64：首轮含建库约 5.85 秒，其余约 2.93–3.02 秒，不包含关系接口的网络耗时。不同手机的实际速度可能不同。

在测试中发现并修正了 BAN 行的隔位校准和重复猜测干扰：最终几何拟合使用整行、同侧英雄去重和清晰锚点优先；并未降低识别接受阈值。

限制：上述 10 例来自同一截图，不是 10 台真实设备或独立对局。断言校验的是去重后的英雄集合，不是每个被水印遮挡槽位的身份。旧截图临时文件部分已失效，本轮未重新测试所有历史截图。新皮肤、遮挡和版本 UI 变化仍需真机回归。

## 界面与行为

- 在模拟器检查五路、四类组合、全对位矩阵和分路校正界面。
- 将镜放入对抗路，原关羽自动换到打野，未出现重复英雄；横竖屏切换后分路校正仍保留。
- 当前标签与阵容使用显式 JSON 状态保存；全对位保留待分路英雄。
- 小样本、缺失关系、负向关系分别可见；未展示算法权重或识别置信度。
- 校正仅作用于分析页；推荐阵容仍在手动 BP 或重新识别中更新。
- 测试 Activity 只在 debug 清单中导出，正式包的分析页不对外导出。
- 正式签名包通过“主页 → 打开手动 BP → 查看阵容对位”入口验证，R8 后 JSON 阵容传递与空阵容页面正常，无运行时崩溃。

可复现入口：`AvatarRegressionActivity`（debug）、`MatchupFixtureActivity`（debug）、`DirectDataFixtureActivity`（debug）。详细头像追踪保存在本机 `work/ocr-qa/v180-final.txt`。
